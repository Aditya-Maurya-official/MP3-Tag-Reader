# MP3-Tag-Reader
This project focuses on extracting and displaying metadata from MP3 audio files by parsing ID3 tags. The application is a terminal-based program that reads MP3 files and retrieves information such as title, artist, album, year, and genre without modifying the file.
